# Server
NODE_ENV=development
PORT=3001
CORS_ORIGIN=http://localhost:5173

# PostgreSQL (source of truth)
DATABASE_URL="postgresql://redis_lab:redis_lab_dev@localhost:5433/redis_patterns_lab?schema=public"

# Redis (optimization + ephemeral state — not yet used for caching in Phase 1)
REDIS_URL="redis://localhost:6380"
